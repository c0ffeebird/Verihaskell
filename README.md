# Verihaskell
Describing Hardware in verilog;systemverilog using Haskell

STEP 1 -> write and understand verilog sintax
STEP 2 -> decide between implementing a custom language or describing verilog using the haskell already built in fefatures
STEP 3 -> ...
